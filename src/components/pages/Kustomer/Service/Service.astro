---
import { getPage, getAllByType } from "@/prismic.js";
import KustomerService from "./Service";

const listService = await getAllByType("service");

const newList = listService.filter((item) =>
    Astro.props.listItems.includes(item.uid),
);
---

<KustomerService {...Astro.props} list={newList} client:visible={{ rootMargin: "100% 0px 100% 0px" }} />
